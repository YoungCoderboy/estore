import React from "react";
const Error = () => {
  return (
    <div className="section-center section text-center">
      <h2>There is error in fetching the data</h2>
    </div>
  );
};

export default Error;
